do `node app.js` to run it (hit localhost:8180)

do you javascript in main.js (starting file)

build your javascript with `watchify  ./main.js -o ./public/javascripts/main.js -v` usees watchify to watch for changes
